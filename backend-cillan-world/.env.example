# Server
HOST=0.0.0.0
PORT=1337
PUBLIC_URL=http://localhost:1337

# Security keys (generate unique values per environment)
APP_KEYS=replace-with-comma-separated-app-keys
API_TOKEN_SALT=replace-with-api-token-salt
ADMIN_JWT_SECRET=replace-with-admin-jwt-secret
TRANSFER_TOKEN_SALT=replace-with-transfer-token-salt
JWT_SECRET=replace-with-user-jwt-secret

# Stripe + frontend integration
STRIPE_KEY=sk_live_or_test_key
CLIENT_URL=https://frontend.example.com

# Database configuration
DATABASE_CLIENT=postgres
DATABASE_HOST=postgres.example.com
DATABASE_PORT=5432
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=super-secure-password
DATABASE_SCHEMA=public
DATABASE_SSL=false

# Optional SMTP / email provider placeholders
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_DEFAULT_FROM=
SMTP_DEFAULT_REPLY_TO=
